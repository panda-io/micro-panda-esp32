name: micro-panda-esp32
version: 0.1.0

src: src/
out: out/

platforms:
  esp32:
    entry_fn: app_main

targets:
  esp32:
    entry: main
    platform: esp32
    flags: [MCU32]
    build_cmd: idf.py build  # for `mpd build esp32`; CMake calls `mpd gen esp32` directly
