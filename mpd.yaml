name: micro-panda-esp32
version: 0.1.0

targets:
  esp32:
    entry: main
    src: src/
    out: main/esp32.c
    config: esp32.yaml
    type: c
    flags: [MCU32]
    build_cmd: idf.py build
    gen:
      entry: app_main
